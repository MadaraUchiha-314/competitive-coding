/*
* Problem Name/URL:
*/
#include <cstdio>
#include <cstdlib>
#include <cstring>
#include <cmath>
#include <vector>
#include <queue>
#include <stack>
#include <utility>
#include <functional>
#include <limits>
#include <map>
#include <set>
#include <iterator>

using namespace std;

#define DEBUG 0

#if DEBUG
  #define debug printf
#else
  #define debug(...) 1 == 1
#endif

#define MAX_NODES 112345
#define MAX_ARRAY_SIZE 112345
#define MAX_MATRIX_SIZE 1123
#define INFINITY_32_BIT (int)~(1<<31) // For 32-Bit
#define INFINITY_64_BIT (long long)~(1<<63) // For 64-Bit
#define MOD 1000000007 // MOD value

// Some function names shortened
#define mp make_pair
#define fi first
#define se second
#define pb push_back
#define sf scanf
#define pf printf
#define sd(n) scanf("%d", &n)
#define slld(n) scanf("%lld", &n)
#define sstr(s) scanf("%s", s)
#define pd(n) printf("%d", n)
#define plld(n) printf("%lld", n)
#define pstr(s) printf("%s", s)
#define pnl() printf("\n")
#define psp() printf(" ")
#define gc() getchar()

// Loops
#define rep(i, N) for (int i = 0; i < N; i++)
#define replr(i, l, r) for (int i = l; i < r; i++)
#define all(x) x.begin(), x.end()
#define printvi(x) for (auto ele: x) pd(ele), psp()
#define printvpi(x) for (auto ele: x) pd(ele.fi), psp(), pd(ele.se), pnl()

// Misc
#define pcodejam(t, answer) pf ("Case :%d %d\n", t + 1, answer)

int main ()
{
  int T;

  return 0;
}
